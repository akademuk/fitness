# Master Performance & SEO Optimization Guide

Этот документ содержит "Мастер-промт" и детальное описание техник, использованных для достижения показателей 100/100 в Google Lighthouse на проекте Elite Fit. Используйте этот гайд для всех будущих проектов.

## 🚀 Master Prompt (Для AI)

Скопируйте этот текст и отправьте AI-ассистенту в начале работы над оптимизацией нового сайта:

```markdown
**Роль:** Ты — эксперт по Web Performance и Technical SEO. Твоя задача — оптимизировать текущий сайт до показателей 100/100 в Google Lighthouse (Mobile & Desktop).

**Требования к оптимизации (Checklist):**

**1. Изображения и Медиа (LCP & CLS):**
*   **Форматы:** Конвертируй все изображения в **WebP**. Используй тег `<picture>` с фоллбэком на JPG/PNG.
*   **Размеры:** У всех тегов `<img>` должны быть явно заданы атрибуты `width` и `height` для предотвращения сдвигов макета (CLS).
*   **Загрузка:**
    *   Первый экран (Hero section): `loading="eager"`, `fetchpriority="high"`.
    *   Остальные изображения: `loading="lazy"`.
*   **Фон:** Если используются фоновые изображения в CSS, замени их на `<img>` с `object-fit: cover`, чтобы браузер мог их лениво загружать.

**2. Шрифты (Render Blocking):**
*   **Стратегия загрузки:** Не используй стандартный `<link rel="stylesheet">` для шрифтов. Используй неблокирующий метод:
    <link rel="stylesheet" href="..." media="print" onload="this.media='all'">
    <noscript><link rel="stylesheet" href="..."></noscript>
*   **Preconnect:** Обязательно добавь `<link rel="preconnect">` для доменов шрифтов (например, fonts.gstatic.com).
*   **JS Синхронизация:** Если используется JS для анимации текста (например, SplitType), оберни инициализацию в `document.fonts.ready.then(() => { ... })`, чтобы избежать пересчета макета (Reflow) до загрузки шрифтов.

**3. JavaScript и Анимации (INP & TBT):**
*   **Загрузка скриптов:** Все скрипты должны иметь атрибут `defer`.
*   **Сторонние библиотеки:** Тяжелые библиотеки (слайдеры, карты, формы) загружай **динамически** только когда они нужны (используй `IntersectionObserver`).
*   **Scroll Performance:**
    *   Никогда не вешай тяжелые вычисления на событие `window.addEventListener('scroll')`. Используй `IntersectionObserver`.
    *   Если используешь GSAP + Lenis (Smooth Scroll), синхронизируй их тикеры: `gsap.ticker.add((time) => { lenis.raf(time * 1000); });`.
*   **Layout Thrashing:** Избегай "Forced Reflow". Не читай геометрические свойства (offsetHeight, getBoundingClientRect) сразу после записи в DOM. Группируй чтения и записи.

**4. CSS:**
*   Минифицируй CSS.
*   Используй `content-visibility: auto` для секций ниже первого экрана, чтобы снизить нагрузку на рендеринг.

**5. Caching & PWA:**
*   Создай `sw.js` (Service Worker) с стратегией **Stale-While-Revalidate** для шрифтов, CSS, JS и изображений. Это обеспечит мгновенную загрузку при повторных визитах.

**6. SEO & Meta:**
*   Добавь **Canonical** ссылку.
*   Настрой **Open Graph** (Facebook) и **Twitter Cards**.
*   Внедри **JSON-LD Structured Data** (Schema.org) для организации (LocalBusiness/Organization).

**Цель:** Зеленая зона (95-100) по всем метрикам Core Web Vitals.
```

---

## 🛠 Детальный разбор примененных техник

### 1. Устранение блокировки рендеринга (Render Blocking Resources)
**Проблема:** Браузер не показывает страницу, пока не скачает CSS шрифтов.
**Решение:**
Мы изменили подключение шрифтов Google Fonts:
```html
<!-- Было -->
<link href="https://fonts.googleapis.com/..." rel="stylesheet">

<!-- Стало (Асинхронно) -->
<link href="https://fonts.googleapis.com/..." rel="stylesheet" media="print" onload="this.media='all'">
<noscript><link href="..." rel="stylesheet"></noscript>
```
**Результат:** Текст отображается мгновенно, шрифты подгружаются фоном.

### 2. Устранение Layout Thrashing (Сдвиги макета из-за JS)
**Проблема:** Скрипт `SplitType` разбивал текст на буквы до того, как загрузился шрифт. Когда шрифт загружался, ширина букв менялась, и браузеру приходилось пересчитывать всю страницу (Reflow), вызывая задержки (50ms+).
**Решение:**
Мы обернули инициализацию в `document.fonts.ready`:
```javascript
document.fonts.ready.then(() => {
    // Инициализация SplitType только после загрузки шрифтов
    new SplitType('[data-split-text]', ...);
});
```
**Результат:** Исчезли "Long Tasks" и "Forced Reflow" в Performance мониторе.

### 3. Оптимизация Scroll-событий
**Проблема:** Слушатели `scroll` срабатывают сотни раз в секунду, перегружая процессор.
**Решение:**
1. Для кнопки "Наверх" (FAB) использовали `IntersectionObserver` вместо `scroll`.
2. Для параллакса и анимаций использовали `GSAP ScrollTrigger`, который оптимизирован лучше нативного скролла.
3. Синхронизировали плавный скролл (Lenis) с анимациями (GSAP) через единый тикер.

### 4. Кэширование (Service Worker)
**Проблема:** При повторном заходе ресурсы скачивались заново.
**Решение:**
Внедрен `sw.js` со стратегией **Stale-While-Revalidate**.
*   Браузер отдает файл из кэша **мгновенно**.
*   В фоне проверяет, не обновился ли файл на сервере.
*   Если обновился — сохраняет новую версию для следующего раза.

### 5. SEO (Search Engine Optimization)
**Внедрено:**
*   **Canonical URL:** Указывает поисковику "главную" версию страницы.
*   **Open Graph / Twitter Cards:** Красивые превью при шеринге в соцсетях.
*   **JSON-LD Schema:** Структурированные данные для Google (адрес, телефон, часы работы), чтобы получить расширенный сниппет в поиске.

## Premium Experience Roadmap (November 2025)

### Audit Summary
- Герой + слайдер используют одинаковые стоковые сюжеты и симметричную копию, поэтому первый экран выглядит как универсальный шаблон.
- Блоки Status Rail, Services, Testimonials повторяют одну карточную сетку, контент лишен персональных метрик/историй.
- Анимации ограничиваются swiper-переходами и базовыми hover-эффектами; отсутствуют сценарные scroll-транзишены и микрообратная связь формы.
- На 768px и ниже типографика перегружает экран, галерея и стат-блоки занимают всю ширину без нюансной иерархии.
- Фирменный язык ограничен текущей палитрой, нет кастомных паттернов, иконок, motion-атрибутов или branded overlays.

### Upgrade Backlog
1. **Immersive hero & section reveals:** внедрить многоуровневый параллакс, stagger-анимацию заголовков и CTA micro-motion при скролле (GSAP + ScrollTrigger, lenis sync).
2. **Premium micro-interactions:** добавить магнитные кнопки, Press/Success состояния в форме, 3D-hover для карточек услуг и timeline-анимацию для статов.
3. **Responsive typographic system:** пересобрать шрифтовые калибровки и spacing для 1440/1024/768/480, внедрить fluid type + контейнерные запросы для галерей.
4. **Brand system:** разработать набор градиентов, текстур (grain/noise, monoline patterns), кастомные иконки и badge-анимацию; вынести переменные в CSS и гайд.
5. **Story-driven content shells:** подготовить шаблон success stories (видео/цифры), интерактивные метрики (progress rings, counters), расширенный coach spotlight.

### Implementation Phases
- **Phase 1 – Visual polish:** ✅ Completed (Responsive typography, new palettes/gradients, updated button and card states).
- **Phase 2 – Motion & interaction:** ✅ Completed (GSAP scenes, magnetic buttons, form micro-interactions, interactive stats rail, 3D card tilt).
- **Phase 3 – Content & storytelling:** 🔄 In Progress (Interactive metrics implemented; success stories & brand assets pending client content).
